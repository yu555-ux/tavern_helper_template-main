完整的前端可交互页面之——第二api，各个页面的设计和收尾，标题页面和开局页面
,
先快速地迅速地讲解一下第二api：
什么是第二API？
,
简而言之，就是一次回复，两个api来共同负责。主api只负责正文的剧情，而副api则负责变量、世界大势、周边变化等信息。
为什么要第二API？
,
很简单，因为模型处理的任务越多，精度就越低，这是显而易见的事实。所以，让剧情的只处理剧情，剩下的部分交给另外一个助手来负责，这就可以显著提高最后的游玩体验。
什么情况下适合采用第二API？
,
如果你的卡足够大，ai需要思考的变量变化足够多、复杂，或者其他的小任务（比如脱离于剧情，还得构思当前世界发生的，和玩家完全无关的大事件；可能玩家这辈子都接触不到的居民A今日的日常；etc.）比较多，那就可以考虑采用第二API。 (已编辑)
接下来开始讲解设计思路：
1.你的前端里，导航栏里应该有一个按钮是“设置”。“设置”可以让你调节前端页面内部的字体大小，样式等。
---
2.在设置里，添加一个模块：输出模式：单api，双api。并简单介绍两个模式的区别：
单API模式
一次输出完整生成剧情+变量
--
多API模式
主API输出剧情，第二API单独处理变量更新和其他的更新
---
3.选择多api后，应该操控世界书，关闭变量相关的所有条目（因为第二api不靠世界书条目读取变量规则，而主api在多api模式下不需要输出变量），比如我的世界书里，我就需要在玩家选择多api模式后，关闭：变量更新规则、变量列表、变量输出格式，单api正文格式、这四个条目,并开启条目 多api正文格式。这样的话，主api就只读取到我规定的，不提及变量更新规范的 多api正文格式 的格式要求了。
---
4.选择多api模式后，应该提供一个配置面板，包含：
API URL（兼容OpenAI格式）
API Key（密码输入）
模型选择（支持手动输入或从API获取列表）
最大重试次数（0-10）
连接测试按钮（测试API连接并显示状态）
获取可用模型按钮（从API获取模型列表）
---
5.多api的流程：区别于目前已经实现的单api，多api里，主api只需要生成和解析除了变量的部分。当主api的</maintext>部分生成完毕后，将最后一次闭合的<maintext></maintext>和其内部的文本，加上变量相关的提示词，一起发送给第二api。这里需要使用的不是generate，而是generateRaw；第二api返回了结果后，合并为一条信息并执行后续的流程。
###  generateRaw，默认不会发送预设、角色描述、世界书、聊天记录等内容到提示词里。基本上是只发送一个孤立的内容。但是你可以自定义apiurl，key，模型，以及要求是否要发送角色描述、聊天记录等信息。
所以通过generateRaw，你需要发送给第二api的内容只需要包含：1.当前的正文，也就是当前主api生成的<maintext>的文本信息；2.变量列表和变量更新规则、变量更新格式；3.其他的任务（比如世界大势？居民生活？）4.第二api的任务（例如，如果你只需要第二api输出变量，那就要求它按照规范，只输出<updateVarlible>就好。）
这样一来，第二api只会读取到当前的正文和变量规范和规则，并且只用输出这部分，因此它的上下文很短，任务很简单和单一，速度会很快，模型要求更低，并且精度会比单一模型在复合任务中输出的变量内容更精准。
